@page
@model IndexModel
@{
    ViewData["Title"] = "Início";
}

<div class="container py-4">
    <section class="marketplace-card p-5 text-center">
        <h1 class="display-5">Marketplace Web</h1>
        <p class="lead mb-0">
            Frontend Razor Pages server-side autenticado por cookie e integrado ao BFF por HTTP com bearer token.
        </p>
    </section>
</div>
